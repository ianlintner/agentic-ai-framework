package com.agenticai.examples.mesh

import zio._
import com.agenticai.mesh._
import com.agenticai.mesh.protocol._

object DistributedAgentMeshExample {
  def main(args: Array[String]): Unit = {
    println("This example has been temporarily disabled to resolve build issues.")
    println("The full functionality will be restored in a future update.")
  }
}